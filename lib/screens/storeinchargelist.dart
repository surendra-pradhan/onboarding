import 'package:flutter/material.dart';

class StoreInchargeList extends StatelessWidget {
  const StoreInchargeList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
